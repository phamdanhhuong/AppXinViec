<UserControl x:Class="AppXinViecWPF.View.Employer.UCTopCV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppXinViecWPF.View.Employer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="45" FontWeight="Medium" Foreground="Black">
                Top CV
            </TextBlock>
        </Grid>
        <Grid Grid.Row="1" Background="LightGray">
            <ContentControl x:Name="ccCompanyList">
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <ItemsControl x:Name="listCv">
                        
                    </ItemsControl>
                </ScrollViewer>
            </ContentControl>
        </Grid>
    </Grid>
</UserControl>
